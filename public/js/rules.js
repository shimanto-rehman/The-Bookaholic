var str = '';

var Question = React.createClass({
	render: function(){
		return(
			
		);
	}
});

var arr = [ 'shahriar', 'subon', 'tt' ];

for( var i = 0; i<3; i++ ){
	React.render(

		<Question />,
		document.getElementById( "shahriar" )[i]

	);
}